server:
  port: 8083

spring:
  application:
    name: category-service

  datasource:
    url: jdbc:mysql://localhost:3306/spring
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
